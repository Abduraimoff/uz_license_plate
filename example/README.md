# uz_license_plate_example

Demo app for the [`uz_license_plate`](../) package.

## Run on a device or simulator

From this directory:

```bash
flutter pub get
flutter run
```

Use `-d android` or `-d ios` to pick a target when several are available.
